# SQL to GORM Skill Assets

This directory contains templates and examples used by the SQL to GORM conversion skill.

## Files

- `template.go` - Go struct template for generated models
- `example_output/` - Example output from conversion

## Usage

Assets in this directory are referenced by the skill but not loaded into context. They serve as:
- Output templates
- Example code snippets
- Formatting references